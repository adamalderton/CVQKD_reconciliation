# Run the band_with_noise.py script with various noise parameters to analyse how 
# the noise affects the guard band properties for a given bit error rate tolerance.

import os
import matplotlib.pyplot as plt